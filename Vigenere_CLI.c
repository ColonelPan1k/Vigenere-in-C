/*
 * Format: vignere 0-[encode, decode] 1-[message] 2-[key]
 */

#include <stdio.h>

char* encode(char* message, char* key);
char* decode(char* message, char* key);

int main(int argc, char* argv[]){
        if (argv[0] == "encode"){
                
        }
        else if (argv[0] == "decode"){
                
        }

        return 0;
}
